{
  "name": "dors",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "dors": "./bin/dors.js"
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@dors/agent": "workspace:*",
    "@dors/ai": "workspace:*",
    "@dors/core": "workspace:*",
    "@dors/tui": "workspace:*",
    "@inquirer/prompts": "^7.0.0",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
